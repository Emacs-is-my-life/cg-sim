* Startup and initialization contract bugs

** DONE Fix trace loader constructor call in simulator startup
CLOSED: [2026-03-30 Mon 10:56]
*** Overview of problem
=Simulator.__init__= constructs the trace loader with only =cfg["trace"]["args"]=, but =TraceLoader.__init__= requires =(obj_id, name, log, args)=. This causes startup failure before trace loading begins.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/simulator.py:Simulator.__init__:52=
- =sim/core/trace/loader.py:TraceLoader.__init__:9=

*** Proposed fix
- Construct the trace loader with explicit simulator-managed identity, name, and logger.
- Keep all trace loader subclasses on the same constructor contract.

** DONE Fix wrong trace loader identity field used by llama.cpp loader
CLOSED: [2026-03-30 Mon 11:02]
*** Overview of problem
=TraceLoader= stores its identifier in =self.id=, but =Llamacpp.load()= constructs =Trace(...)= using =self.obj_id=. That attribute does not exist, so trace loading raises =AttributeError= even if loader construction is fixed.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/trace/loader.py:TraceLoader.__init__:10=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:145=

*** Proposed fix
- Replace =self.obj_id= with =self.id= in =Llamacpp.load()=.
- Standardize object identity field naming across loader and sim objects.

** DONE Fix memory hardware construction to pass owner into MemorySpace
CLOSED: [2026-03-30 Mon 11:08]
*** Overview of problem
=MemorySpace.__init__= requires =(hw, num_total_pages)=, but =BaseMemory.__init__= passes only the page count. Any memory hardware instantiation fails during startup.

*** Related =<file_name>:<function_name>:<line>=
- =sim/hw/memory/common/base_memory.py:BaseMemory.__init__:16=
- =sim/hw/memory/common/memory_region.py:MemorySpace.__init__:28=

*** Proposed fix
- Construct memory space as =MemorySpace(self, KB_to_num_pages(memory_size_KB))=.
- Keep the owning hardware explicit in region-space objects.

** DONE Fix storage hardware construction to pass owner into StorageSpace
CLOSED: [2026-03-30 Mon 11:08]
*** Overview of problem
=StorageSpace.__init__= requires the owning storage hardware, but =BaseStorage.__init__= calls =StorageSpace()= with no arguments. Storage hardware instantiation fails during startup.

*** Related =<file_name>:<function_name>:<line>=
- =sim/hw/storage/common/base_storage.py:BaseStorage.__init__:13=
- =sim/hw/storage/common/storage_region.py:StorageSpace.__init__:20=

*** Proposed fix
- Construct storage space as =StorageSpace(self)= in =BaseStorage.__init__=.
- Keep region ownership centralized in =StorageSpace=.

** DONE Avoid giving schedulers a partially wired System instance
CLOSED: [2026-03-30 Mon 11:10]
*** Overview of problem
=System.engine= is initialized to =None= and only injected inside =Engine.__init__=. However, scheduler instances are constructed before the engine. Any scheduler constructor that submits work via =System= will fail because =self.engine.submit(...)= is unavailable.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/system.py:System.__init__:23=
- =sim/core/simulator.py:Simulator.__init__:75=
- =sim/core/simulator.py:Simulator.__init__:78=
- =sim/core/engine/engine.py:Engine.__init__:26=
- =sim/core/system.py:System.compute:28=
- =sim/core/system.py:System.claim:33=
- =sim/core/system.py:System.release:38=
- =sim/core/system.py:System.transfer:43=

*** Proposed fix
- Instantiate =Engine= before constructing the scheduler, or inject the engine into =System= before any scheduler can use =System= submission APIs.
- Keep scheduler constructors side-effect free if construction order must remain.

* Engine and scheduling runtime bugs

** TODO Prevent infinite runtime loop when built-in schedulers do nothing
*** Overview of problem
Both built-in schedulers leave =layout()= and =runtime()= as no-ops. In =Engine._runtime()=, when no jobs are submitted and no terminal job can ever retire, the loop never terminates and never raises.

*** Related =<file_name>:<function_name>:<line>=
- =sim/sched/vanilla/vanilla.py:Vanilla.layout:24=
- =sim/sched/vanilla/vanilla.py:Vanilla.runtime:27=
- =sim/sched/flexgen/flexgen.py:FlexGen.layout:23=
- =sim/sched/flexgen/flexgen.py:FlexGen.runtime:26=
- =sim/core/engine/engine.py:Engine._runtime:93=

*** Proposed fix
- Replace placeholder =pass= methods with =NotImplementedError= until a real scheduler is implemented.
- Ensure at least one scheduler submits initial jobs required to reach the terminal node.

** TODO Fix missing ETA assignment for instant claim/release jobs
*** Overview of problem
=ClaimJob= and =ReleaseJob= are instantaneous jobs with =work_total = 0=, but =update_running_jobs()= assigns ETAs only for =ComputeJob= and =TransferJob=. Instant jobs therefore keep =timestamp_ETA = None= after being appended to the running heap, which breaks ordering and time advancement.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/job/claim_job.py:ClaimJob.__init__:18=
- =sim/core/job/release_job.py:ReleaseJob.__init__:16=
- =sim/core/engine/update.py:update_running_jobs:11=
- =sim/core/engine/engine.py:Engine._runtime:118=
- =sim/core/job/job.py:BaseJob.__lt__:25=
- =sim/core/engine/engine.py:Engine._runtime_forward:79=

*** Proposed fix
- In =update_running_jobs()=, explicitly assign ETA for instant jobs to =timestamp_now=.
- Keep heap ordering valid for every job type.

** TODO Make empty-engine runtime failure explicit instead of spinning forever
*** Overview of problem
=Engine._runtime()= loops forever even when both =job_waiting= and =job_running= are empty and the scheduler submitted nothing. This is a non-terminating execution state distinct from deadlock detection, because the current deadlock check only runs when a blocked waiting job exists.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/engine/engine.py:Engine._runtime:93=

*** Proposed fix
- Add an explicit failure path when both queues are empty after scheduler processing and no terminal node has completed.
- Report this as a scheduler contract violation or invalid execution state.

** TODO Clarify and implement layout-phase execution semantics
*** Overview of problem
The simulator exposes both =Engine._layout()= and =Engine._layout_forward()=. Scheduler APIs can submit jobs through =System= during layout, but =Engine._layout()= only calls =sched.layout()= and never drains or retires submitted jobs. Layout-submitted jobs therefore leak into runtime semantics unexpectedly.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/engine/engine.py:Engine._layout_forward:58=
- =sim/core/engine/engine.py:Engine._layout:74=
- =sim/sched/common/base_scheduler.py:BaseScheduler.layout:18=
- =sim/core/system.py:System.claim:33=
- =sim/core/system.py:System.release:38=
- =sim/core/system.py:System.transfer:43=

*** Proposed fix
- Choose one model and enforce it:
  - either layout is planning only and must not submit jobs, or
  - layout is simulated execution and =Engine._layout()= must process submitted layout jobs deterministically.
- Keep layout and runtime behavior separated by contract.

** TODO Revisit strict FIFO waiting-queue semantics that create false deadlocks
*** Overview of problem
=Engine._runtime()= drains =job_waiting= strictly from the left and stops at the first blocked job. If that head job is unrunnable while no job is currently running, the engine raises deadlock even if later queued jobs are runnable and could make progress. This creates a hidden scheduler ordering constraint.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/engine/engine.py:Engine._runtime:106=
- =sim/core/system.py:System.compute:28=
- =sim/core/system.py:System.claim:33=
- =sim/core/system.py:System.release:38=
- =sim/core/system.py:System.transfer:43=

*** Proposed fix
- Either document strict submission order as part of the scheduler contract, or allow later runnable jobs to bypass blocked earlier jobs.
- Prefer one explicit policy and enforce it consistently.

* Job lifecycle and mutation bugs

** DONE Remove duplicated hardware retirement in job end path
CLOSED: [2026-03-30 Mon 12:49]
*** Overview of problem
=BaseJob.end()= retires the job from every hardware in =running_on= before calling =end_mut()=. Several mutation hooks also retire the same job again. This duplicates lifecycle responsibility and obscures who owns hardware bookkeeping.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/job/job.py:BaseJob.end:76=
- =sim/core/job/mutation/claim_mutation.py:end_mutation:20=
- =sim/core/job/mutation/compute_mutation.py:end_mutation:41=
- =sim/core/job/mutation/release_mutation.py:end_mutation:16=

*** Proposed fix
- Keep generic hardware retirement only in =BaseJob.end()= and remove duplicate retire loops from mutation hooks.
- Restrict mutation hooks to domain-state changes.

** DONE Register transfer jobs on participating hardware
CLOSED: [2026-03-30 Mon 12:50]
*** Overview of problem
Transfer mutation updates region access flags but never calls =hw.run(job)= on the source and destination hardware. As a result, hardware occupancy and bandwidth models never see active transfers, so ETA calculations can run against stale hardware state.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/job/mutation/transfer_mutation.py:begin_mutation:7=
- =sim/hw/common/base_hardware.py:BaseHardware.run:28=
- =sim/hw/storage/simple_ssd/simple_ssd.py:SimpleSSD.update_work_rate:75=

*** Proposed fix
- In =begin_mutation()=, call =hw.run(job)= for every hardware in =job.running_on= before changing region state.
- Keep hardware retirement centralized in =BaseJob.end()=.

** DONE Fix invalidation order for compute outputs
CLOSED: [2026-03-30 Mon 12:50]
*** Overview of problem
=compute_mutation.begin_mutation()= marks the output memory region as =BEING_WRITTEN= and then immediately calls =invalidate(sys, o_tensor_id)= for the same tensor ID. If invalidation traverses all copies of that tensor, it can invalidate the very destination region that was just selected to receive the new output, corrupting freshness/readiness state for the active write target.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/job/mutation/compute_mutation.py:begin_mutation:26=
- =sim/core/job/mutation/compute_mutation.py:begin_mutation:30=
- =sim/core/job/mutation/utils.py:invalidate:<line not provided in chat>=

*** Proposed fix
- Exclude the destination output region from invalidation, or invalidate stale copies before marking the chosen destination region as the new write target.
- Keep exactly one latest output copy after the write completes.

** DONE Fix transfer job construction to reject empty batches
CLOSED: [2026-03-30 Mon 12:51]
*** Overview of problem
=TransferJob.__init__= unconditionally reads =batch[0]=. An empty transfer batch therefore raises =IndexError= at job construction time.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/job/transfer_job.py:TransferJob.__init__:20=

*** Proposed fix
- Validate that =batch= is non-empty and raise a clear exception before dereferencing the first element.
- Keep transfer job invariants explicit at construction.

* Trace loading and trace-model correctness bugs

** DONE Fix llama.cpp record path normalization check
CLOSED: [2026-03-30 Mon 13:02]
*** Overview of problem
When normalizing the CSV record path, =Llamacpp.load()= checks =dot_graph_path.is_absolute()= instead of =record_path.is_absolute()=. Relative record paths can therefore be handled incorrectly.

*** Related =<file_name>:<function_name>:<line>=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:24=

*** Proposed fix
- Change the condition to =if not record_path.is_absolute():= before joining with =input_dir=.

** DONE Remove hardcoded llama.cpp step count
CLOSED: [2026-03-30 Mon 13:01]
*** Overview of problem
The loader hardcodes =step_till = 10= and expands the compute graph for exactly 11 steps regardless of input data. This produces incorrect traces for any profile with a different step range.

*** Related =<file_name>:<function_name>:<line>=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:95=

*** Proposed fix
- Derive the available steps from the CSV contents and iterate actual data instead of a fixed constant.

** DONE Replace fragile positional CSV indexing in llama.cpp loader
CLOSED: [2026-03-30 Mon 13:01]
*** Overview of problem
The loader extracts row fields from =iter_rows()= using hardcoded tuple offsets. If the CSV column order changes, node IDs, names, and timings are silently misparsed.

*** Related =<file_name>:<function_name>:<line>=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:103=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:104=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:105=

*** Proposed fix
- Read named columns explicitly instead of positional tuple indices.
- Validate required schema before expanding the trace.

** DONE Fix incorrect tensor-type detection for canonical llama.cpp input labels
CLOSED: [2026-03-30 Mon 13:03]
*** Overview of problem
=get_tensor_type()= checks =elif "inp_embd" == label:= only on the raw label string. Real labels are parsed elsewhere through canonicalization and often contain suffixes or metadata, so input tensors can be misclassified and the metadata becomes wrong at load time.

*** Related =<file_name>:<function_name>:<line>=
- =sim/load/llamacpp/utils.py:get_tensor_type:12=
- =sim/load/llamacpp/utils.py:node_name_canonicalizer:7=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:38=

*** Proposed fix
- Derive tensor type from the canonicalized name or robust label parsing, not exact raw-label equality against one special string.
- Keep type inference consistent with the label normalization pipeline.

** TODO Review tensor identity policy across repeated llama.cpp steps
*** Overview of problem
During per-step node expansion, all copied nodes reuse the same tensor IDs taken from the virtual graph. That is valid only for truly persistent tensors. For step-local intermediates, values from different steps become indistinguishable and placement/invalidation semantics become incorrect.

*** Related =<file_name>:<function_name>:<line>=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:127=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:130=
- =sim/load/llamacpp/llamacpp.py:Llamacpp.load:133=
- =sim/load/llamacpp/utils.py:get_tensor_type:13=
- =sim/core/trace/tensor.py:Tensor.__init__:18=

*** Proposed fix
- Define tensor ID policy by tensor lifetime class:
  - persistent tensors may reuse IDs across steps,
  - step-local intermediates should receive distinct per-step IDs.
- Use =tensor_type= metadata to drive this mapping explicitly.

** DONE Strengthen Trace structural validation for parent and child node references
CLOSED: [2026-03-30 Mon 13:04]
*** Overview of problem
=map_check()= validates tensor references and terminal-node placement, but does not verify that parent and child node IDs referenced by each node actually exist in =node_map=. Broken graph dependencies can therefore pass validation and fail later during scheduling or execution.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/trace/trace.py:map_check:10=
- =sim/core/trace/node.py:Node.add_parent_node:37=
- =sim/core/trace/node.py:Node.add_child_node:48=

*** Proposed fix
- Extend =map_check()= to validate that every parent and child node ID exists in =node_map=.
- Keep terminal-node validation separate from per-node structural checks.

** DONE Move terminal-node validation out of the per-node loop
CLOSED: [2026-03-30 Mon 13:05]
*** Overview of problem
=map_check()= re-fetches and re-validates the last node inside the loop over every node. This repeats the same validation unnecessarily and mixes per-node checks with whole-graph termination checks.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/trace/trace.py:map_check:21=

*** Proposed fix
- Perform terminal-node validation once after the per-node loop completes.

** DONE Fix invalid TensorWithSign conversion helper
CLOSED: [2026-03-30 Mon 13:06]
*** Overview of problem
=TensorWithSign.get_Tensor()= returns =Tensor(self._id, self.name, self.type, self.size_bytes, self.args)=, but =Tensor= has no =_id= or =type= fields and its constructor does not accept that argument order. If this helper is ever called, it raises immediately.

*** Related =<file_name>:<function_name>:<line>=
- =sim/load/llamacpp/utils.py:TensorWithSign.get_Tensor:31=
- =sim/core/trace/tensor.py:Tensor.__init__:18=

*** Proposed fix
- Either remove the unused helper or rewrite it to construct =Tensor(self.id, self.name, self.size_bytes, self.args)=.
- Keep helper methods aligned with the current Tensor API.

* Memory, storage, and hardware-state bugs

** DONE Fix MemorySpace.release so it actually removes the allocation
CLOSED: [2026-03-30 Mon 12:55]
*** Overview of problem
=MemorySpace.release()= decrements page usage but only deletes the local variable =free_region= instead of removing the matching entry from =_regions_by_page_idx_start=. Released memory remains allocated in the region map forever.

*** Related =<file_name>:<function_name>:<line>=
- =sim/hw/memory/common/memory_region.py:MemorySpace.release:108=

*** Proposed fix
- Remove the matching entry from =self._regions_by_page_idx_start= by key.
- Update =num_used_pages= using the removed region’s page count.

** TODO Reset SSD work-rate counters when device becomes idle
*** Overview of problem
=SimpleSSD.update_work_rate()= updates =read_from= and =write_to= only when a job is running. When the SSD becomes idle, prior values remain latched, leaking stale bandwidth into later ETA calculations and logs.

*** Related =<file_name>:<function_name>:<line>=
- =sim/hw/storage/simple_ssd/simple_ssd.py:SimpleSSD.update_work_rate:75=

*** Proposed fix
- In the idle path, explicitly reset =read_from= and =write_to= to zero.

** DONE Align SSD model implementation with its documented fixed-latency behavior
CLOSED: [2026-03-30 Mon 12:56]
*** Overview of problem
The =SimpleSSD= docstring says IO latency includes a fixed-latency term, but the implementation only derives throughput from the IO curve and never incorporates =fixed_latency_micros= into transfer timing. The documented model and runtime behavior do not match.

*** Related =<file_name>:<function_name>:<line>=
- =sim/hw/storage/simple_ssd/simple_ssd.py:SimpleSSD.__init__:19=
- =sim/hw/storage/simple_ssd/simple_ssd.py:SimpleSSD.update_work_rate:75=
- =sim/hw/storage/simple_ssd/simple_ssd.py:SimpleSSD._get_bandwidth_KBps:52=

*** Proposed fix
- Either incorporate fixed latency into transfer ETA calculation, or remove the fixed-latency claim from the storage model contract.
- Keep documentation and simulated behavior consistent.

** DONE Fix inconsistent compute counter key names
CLOSED: [2026-03-30 Mon 12:57]
*** Overview of problem
=BaseCompute.log_counters()= returns ={"compute_speed": 0}= when idle, but emits =compute_speed_AUps= when busy. Counter consumers therefore see different schemas depending on runtime state.

*** Related =<file_name>:<function_name>:<line>=
- =sim/hw/compute/common/base_compute.py:BaseCompute.log_counters:23=

*** Proposed fix
- Use one stable counter key in both branches, such as =compute_speed_AUps=.

* Logging and observability bugs

** DONE Harden logger worker thread against serialization and IO failures
CLOSED: [2026-03-30 Mon 12:59]
*** Overview of problem
The log writer thread performs queue draining, JSON serialization, and file IO with no exception handling. Any failure in =_flush()= or =_close_file()= can kill the worker thread silently while the simulation continues, producing incomplete or corrupt output without surfacing the failure.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/log/log.py:Log._flush:58=
- =sim/core/log/log.py:Log._run:75=
- =sim/core/log/log.py:Log.stop:109=

*** Proposed fix
- Capture worker thread exceptions, store them on the logger, and re-raise or surface them from =stop()=.
- Protect final flush and file close in the shutdown path.

** DONE Replace full-trace state snapshots with transition logging or gated debug snapshots
CLOSED: [2026-03-30 Mon 12:59]
*** Overview of problem
=Trace.log_states()= serializes the status of every node into one snapshot. On large traces this creates oversized logs and expensive repeated serialization during normal execution, which becomes a real scalability problem.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/trace/trace.py:Trace.log_states:48=
- =sim/core/job/mutation/compute_mutation.py:begin_mutation:18=
- =sim/core/job/mutation/compute_mutation.py:end_mutation:43=

*** Proposed fix
- Log node status transitions as events when states change, and reserve full trace snapshots for debug mode only.

* Type/import and dependency bugs

** DONE Remove runtime circular imports used only for type annotations
CLOSED: [2026-03-30 Mon 12:59]
*** Overview of problem
Several modules import each other at runtime only to express type annotations, creating circular import risk across =core=, =engine=, =memory=, =storage=, and region modules. Current examples include =data_region.py= importing both storage and memory base classes while those modules import region types back.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/system.py:=module import section:1=
- =sim/core/engine/engine.py:=module import section:1=
- =sim/hw/common/data_region.py:=module import section:1=
- =sim/hw/memory/common/base_memory.py:=module import section:1=
- =sim/hw/memory/common/memory_region.py:=module import section:1=
- =sim/hw/storage/common/base_storage.py:=module import section:1=
- =sim/hw/storage/common/storage_region.py:=module import section:1=

*** Proposed fix
- Move annotation-only imports behind =TYPE_CHECKING= and use postponed or string annotations where appropriate.
- Keep runtime imports only where execution depends on them.

** DONE Remove unnecessary NumPy dependency from Tensor page-count calculation
CLOSED: [2026-03-30 Mon 12:59]
*** Overview of problem
=Tensor.__init__= imports NumPy solely to compute a ceiling over page-size division. This adds a heavyweight dependency to tensor creation and can fail otherwise valid environments that do not need NumPy.

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/trace/tensor.py:=module import section:2=
- =sim/core/trace/tensor.py:Tensor.__init__:27=

*** Proposed fix
- Replace the NumPy ceiling call with integer ceiling division.
- Remove the NumPy import from this module.

* Determinism and ordering bugs

** DONE Replace UUID-based heap tie-breaking with explicit monotonic sequence
CLOSED: [2026-03-30 Mon 12:59]
*** Overview of problem
=BaseJob.__lt__= orders jobs by =(timestamp_ETA, id)=. When ETAs tie, the heap falls back to UUID ordering, which is effectively arbitrary and makes execution order non-deterministic across runs.a

*** Related =<file_name>:<function_name>:<line>=
- =sim/core/job/job.py:BaseJob.__lt__:25=

*** Proposed fix
- Add a monotonic creation sequence number to =BaseJob= and compare =(timestamp_ETA, sequence)= instead.
- Keep heap behavior deterministic and simulator results reproducible.
